{% extends '@Wcm/Layout/shared/page_base.html.twig' %}

{% block title %}user{% endblock %}
{% block subtitle %}{% endblock %}
{% block content %}


    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ user.id }}</td>
        </tr>
        <tr>
            <th>Username</th>
            <td>{{ user.username }}</td>
        </tr>
        <tr>
            <th>Password</th>
            <td>{{ user.password }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_wcm_users') }}">back to list</a>

    <a href="{{ path('app_wcm_edit_user', {'id': user.id}) }}">edit</a>

    {{ include('@Wcm/user/_delete_form.html.twig') }}
{% endblock %}

